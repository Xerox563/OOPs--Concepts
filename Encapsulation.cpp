#include <bits/stdc++.h>
using namespace std;

class Accoutn
{
    /* Data Hiding : Make the Sensitive information as private so that no one can access it..*/
private:
    double balance;
    string password;

public:
    string accountId;
    string username;
};
int main()
{
    // object creation -> className objectName;

    return 0;
}

/*
 * Encapsulation is wrapping up of data and member functions in a single unit called class..
 * Capsule of (Data / Properites + Member Functions)
 * Ek Class bnanaa aur usmae data member + member functions ko declare krna hi encapsulation hota hai
 * Data Hiding -> make sensitive information as private
 */